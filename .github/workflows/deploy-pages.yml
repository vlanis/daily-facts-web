name: Deploy Pages

# Publishes the site/ directory to GitHub Pages. Runs on site changes, after
# each daily generation (so the freshly committed today.json goes live), and
# on manual dispatch. See specs/ui.md.

on:
  push:
    branches: [main]
    paths:
      - "site/**"
  workflow_run:
    workflows: ["Daily generate"]
    types: [completed]
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload site/ artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
